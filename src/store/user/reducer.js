const initialState = {
    username: "Jeroen",
    profileImg: "link to img"
}

export default (state = initialState, { type, payload }) => {
    switch (type) {

 
    default:
        return state
    }
}
